# Copy to deploy.env on the server (not committed to git):
#   cp scripts/deploy.env.example scripts/deploy.env
#   chmod 600 scripts/deploy.env

DEPLOY_SUDO_PASSWORD=your-sudo-password-here
GUNICORN_SERVICE=gunicorn
GIT_BRANCH=main
